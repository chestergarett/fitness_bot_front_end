import Drawer from '../Drawer/Drawer';

const Notif = () => {
    return(
        <Drawer>
            Insert not started, no records in the past dates.
        </Drawer>
    )
}

export default Notif;